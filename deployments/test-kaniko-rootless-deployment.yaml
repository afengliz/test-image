apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-kaniko-rootless-deployment
  namespace: ones
  labels:
    app: test-kaniko-rootless
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-kaniko-rootless
  template:
    metadata:
      labels:
        app: test-kaniko-rootless
    spec:
      containers:
      - name: test-kaniko-rootless
        image: localhost:5000/new-kaniko-rootless-image:latest
        imagePullPolicy: IfNotPresent
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"

